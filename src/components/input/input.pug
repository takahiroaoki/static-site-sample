mixin input(ref, id)
    //-
        attributes:
            type: input type, default 'text'
            placeholder: input placeholder, default 'placeholder'
    - var { placeholder = 'placeholder', type = 'text' } = {...attributes}
    .input(data-ref=ref)
        input.input__content(id=id type=type placeholder=placeholder)
        span.input__message